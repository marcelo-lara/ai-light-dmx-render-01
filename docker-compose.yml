services:
  backend:
    build: ./backend
    volumes:
      - ./data/fixtures:/app/fixtures:ro
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend
